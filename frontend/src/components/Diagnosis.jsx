function Diagnosis({ record }) {
  const diagnosis = record.diagnosis || {};
  return <div className="diagnosis-grid"><section className="panel diagnosis-main"><div className="result-top"><div><span className="eyebrow">AI ASSESSMENT</span><h2>{diagnosis.likely_root_cause || 'Unable to determine'}</h2></div><span className={`confidence ${String(diagnosis.status || 'unknown').toLowerCase()}`}>{diagnosis.status || 'UNKNOWN'} <b>{Math.round((diagnosis.confidence || 0) * 100)}%</b></span></div><div className="meta-row"><span>{diagnosis.classification || 'UNKNOWN'}</span><span>{diagnosis.osi_layer || 'Unknown'}</span><span>{diagnosis.severity || 'Unknown'} severity</span></div><div className="callout"><strong>Human review required before applying any fix.</strong><p>{diagnosis.recommended_fix || 'Collect additional show-command evidence.'}</p></div><div className="two-columns"><div><h3>Evidence</h3><ul>{(diagnosis.evidence || []).map((item, index) => <li key={index}>{typeof item === 'string' ? item : JSON.stringify(item)}</li>)}</ul></div><div><h3>Alternative causes</h3><ul>{(diagnosis.alternative_causes || []).map((item, index) => <li key={index}>{item}</li>)}{!diagnosis.alternative_causes?.length && <li>No alternatives returned.</li>}</ul></div></div></section><Commands commands={diagnosis.next_commands || []} /></div>;
}
function Commands({ commands }) { return <section className="panel commands"><span className="eyebrow">COLLECT NEXT</span><h3>Recommended commands</h3>{commands.map((command, index) => <code key={index}>{command}</code>)}{!commands.length && <p>No command recommendation returned.</p>}</section>; }
export default Diagnosis;
