function History({ records, onOpen }) { return <section className="panel history-table"><div className="section-title"><h3>Recent investigations</h3><span>{records.length} cases</span></div>{records.length ? <div className="table-wrap"><table><thead><tr><th>Case</th><th>Classification</th><th>Root cause</th><th>Confidence</th><th>Review</th></tr></thead><tbody>{records.map((record) => <tr key={record.case_id} onClick={() => onOpen(record.case_id)}><td><strong>{record.case_id}</strong><small>{new Date(record.timestamp).toLocaleString()}</small></td><td>{record.diagnosis?.classification || 'UNKNOWN'}</td><td>{record.diagnosis?.likely_root_cause || 'Unable to determine'}</td><td>{Math.round((record.diagnosis?.confidence || 0) * 100)}%</td><td><span className="review-status">{record.human_review?.status || 'pending'}</span></td></tr>)}</tbody></table></div> : <p className="muted">No investigations stored yet.</p>}</section>; }
export default History;
